# قائمة الإطلاق الرسمي — HR Pro 2.0.0

الترتيب مهم. كل خطوة مكتوب جنبها **مَن ينفذها**:
- **أنت**: لأنها تحتاج تسجيل دخول أو كلمة سر (Claude ما يدخل كلمات سرك).
- **Claude**: يقدر يسويها بعد ما تخلص خطوتك.

---

## 1) قاعدة البيانات — أولاً وقبل كل شي

> بدونها: الموقع الجديد والتطبيق الجديد ما يشتغلون صح (اعتماد الرواتب والسلف، إضافة
> موظف، البصمة الآمنة...). والتطبيق القديم الموجود عند الموظفين يتوقف عن البصمة بعدها،
> لذلك طبّقها **بنفس اليوم** اللي توزّع بيه التطبيق الجديد (الخطوة 4).

| # | الخطوة | مَن |
|---|---|---|
| 1.1 | `npx supabase login` وأكمل الدخول بالمتصفح | أنت |
| 1.2 | `npx supabase link --project-ref jgjlmddphhncatrhqrej` واكتب كلمة سر القاعدة | أنت |
| 1.3 | نسخة احتياطية كاملة من القاعدة (مخطط + بيانات) | Claude |
| 1.4 | فحص: أي تحديثات مطبّقة وأيها جديدة، وتجربة جافة `db push --dry-run` | Claude |
| 1.5 | موافقتك الصريحة ثم `supabase db push` | أنت توافق، Claude ينفذ |
| 1.6 | فحص الأمان بعد التطبيق (`supabase/checks/security_audit.sql`) | Claude |

## 2) مفتاح التوقيع الرسمي للأندرويد (مرة وحدة بالعمر)

| # | الخطوة | مَن |
|---|---|---|
| 2.1 | شغّل: `powershell -ExecutionPolicy Bypass -File mobile\scripts\create_release_key.ps1` | أنت |
| 2.2 | انسخ `mobile\android\app\release.jks` وكلمات السر على فلاش ومدير كلمات سر | أنت |
| 2.3 | في GitHub → Settings → Secrets أضف: `ANDROID_KEYSTORE_BASE64` (من ملف `release.jks.base64`)، `ANDROID_KEYSTORE_PASSWORD`، `ANDROID_KEY_PASSWORD`، `ANDROID_KEY_ALIAS` | أنت |

> ⚠️ ضياع هذا المفتاح = ما تقدر تحدّث التطبيق عند الموظفين بعد. احفظه بمكانين.

## 3) بناء التطبيق الرسمي

| # | الخطوة | مَن |
|---|---|---|
| 3.1 | `flutter build apk --release --split-per-abi` والتأكد إنه موقّع بالمفتاح الرسمي | Claude |
| 3.2 | تجربة على هاتف حقيقي (القائمة بالأسفل) | أنت |

## 4) التوزيع على الموظفين (نفس يوم الخطوة 1.5)

| # | الخطوة | مَن |
|---|---|---|
| 4.1 | تصفير قفل الأجهزة مرة وحدة (التوقيع الجديد يغيّر معرّف الجهاز) — الأمر في `DEVELOPER_GUIDE.md` | Claude بعد موافقتك |
| 4.2 | رفع ملف APK إلى التخزين `ota-updates` وإضافة سطر في `app_versions` كتحديث إجباري | Claude بعد موافقتك |
| 4.3 | إبلاغ الموظفين: **احذف التطبيق القديم أولاً** ثم نصّب الجديد (اختلاف التوقيع) | أنت |
| 4.4 | دمج الـ PR على GitHub → الموقع يُنشر تلقائياً | أنت |

## 5) آيفون

- يحتاج حساب Apple Developer (عندك)، وإعداد Firebase للآيفون، وشهادة توقيع — التفاصيل في `IOS_SETUP.md`.
- التوزيع الخاص (Ad Hoc) محدود بـ 100 جهاز آيفون. للأكثر: TestFlight أو App Store (نشر خاص للشركة).
- سياسة الخصوصية جاهزة على: `https://batra-hr-pro.surge.sh/privacy` (تظهر بعد دمج الـ PR)، وApple تطلب رابطها.

---

## تجربة قبل التوزيع (هاتف حقيقي)

- [ ] تسجيل الدخول، وتغيير كلمة المرور لحساب جديد
- [ ] بصمة حضور **داخل** الفرع ← تنجح، و**خارج** الفرع ← ترفض برسالة واضحة
- [ ] بصمة بدون إنترنت ← تُحفظ وتُرسل تلقائياً لما يرجع الإنترنت
- [ ] بصمة انصراف، والرئيسية تعرض "اكتمل الدوام"
- [ ] طلب إجازة وطلب سلفة (مع تصوير التعهد)
- [ ] من حساب مدير: قبول/رفض الإجازة والسلفة، ووصول الإشعار للموظف
- [ ] التتبع الحي يعرض الموظف على الخريطة
- [ ] كشف الراتب وتصدير PDF
- [ ] الإشعارات تصل والتطبيق مغلق
