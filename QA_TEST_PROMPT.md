# برومبت فحص شامل — HR Pro 2.0 (Android + iPhone)

> انسخ كل ما تحت الخط وأعطه لمختبِر (إنسان أو وكيل ذكاء اصطناعي عنده وصول للهاتف/المحاكي).
> عدّل قسم "بيئة الفحص" فقط قبل الإرسال.

---

## الدور

أنت مهندس ضمان جودة (QA) أول متخصص في تطبيقات Flutter للموارد البشرية وأنظمة الحضور بالموقع الجغرافي.
مهمتك: استعمال تطبيق **HR Pro** كمستخدم حقيقي بكل أدواره، على **Android و iPhone**، واكتشاف كل خطأ
وظيفي أو أمني أو في الواجهة أو في الإشعارات، ثم كتابة تقرير أخطاء دقيق قابل للتنفيذ.
لا تفترض أن أي شيء يعمل — جرّبه وأثبته. لا تصلح شيئاً بنفسك؛ وثّق فقط.

## بيئة الفحص (يعبّئها صاحب المشروع)

| البند | القيمة |
|---|---|
| نسخة التطبيق | 2.0.0 (build 4) |
| أجهزة Android | مثال: Samsung A-series (Android 14)، Xiaomi (Android 13)، جهاز قديم Android 10 |
| أجهزة iPhone | مثال: iPhone 12 (iOS 17)، iPhone 15 (iOS 18) |
| لوحة الويب للإدارة | https://batra-hr-pro.surge.sh |
| حسابات الاختبار | موظف، مدير (manager)، أدمن/HR (admin) — **حسابات اختبار فقط**، تُعطى للمختبِر بشكل منفصل |
| الفرع التجريبي | اسم الفرع، إحداثياته، نصف قطر النطاق بالمتر |
| دوام الفرع | مثال: 09:00–14:00، أيام العمل الأحد–الخميس + السبت، سماح 5 دقائق، تذكير بعد 5 دقائق |

قواعد: لا تستعمل بيانات موظفين حقيقيين، لا تكتب كلمات السر في التقرير، وأي موظف/سلفة/إجازة تنشئها للفحص
ابدأ اسمها بـ `QA-` حتى تُحذف بعد الانتهاء.

## ما هو التطبيق (مرجع لتوقّع السلوك الصحيح)

- تطبيق Flutter عربي (RTL) **بالوضع الداكن فقط** — لا يوجد وضع فاتح إطلاقاً.
- الخلفية Supabase: كل العمليات الحساسة تتم على السيرفر (RPC) وليس في التطبيق:
  - البصمة `punch_attendance`: تتحقق من المسافة عن الفرع، كشف الموقع الوهمي، ربط الجهاز، ووقت البصمة.
  - الأدوار: `employee` / `manager` / `admin`. صلاحيات الكتابة على الموظفين والسلف والرواتب والجداول **للأدمن فقط**.
- الإشعارات: Firebase Cloud Messaging عبر دالة `push-notification`، تُطلق عند إضافة أي صف في جدول الإشعارات.
- التذكيرات تُرسل من السيرفر كل دقيقة (pg_cron) حسب جدول دوام الموظف.
- التتبع الحي للموقع **أثناء ساعات الدوام فقط** وبإشعار ظاهر دائماً (غير مخفي).
- التحديث داخل التطبيق (OTA) لـ Android عبر جدول `app_versions`.

---

## خطة الفحص

نفّذ كل حالة على **منصتين على الأقل (Android + iPhone)** وسجّل النتيجة: ✅ نجح / ❌ فشل / ⚠️ يعمل مع ملاحظة.

### 1) التنصيب والتشغيل الأول
1. تنصيب نظيف، وتنصيب فوق نسخة قديمة (Android: توقيع مختلف يجب أن يرفض التنصيب فوقها — تأكد من وضوح ذلك).
2. شاشة البداية (Splash) داكنة بدون وميض أبيض، على الوضع الفاتح للنظام أيضاً.
3. طلبات الصلاحيات بالترتيب ونصوصها مفهومة بالعربي:
   - الموقع (أثناء الاستخدام ثم "دائماً" على iPhone / "السماح طوال الوقت" على Android).
   - الإشعارات (Android 13+ و iOS).
   - تحسين البطارية على Android (استثناء التطبيق).
4. رفض كل صلاحية ثم استعمال التطبيق: هل تظهر رسالة واضحة وزر لفتح الإعدادات؟ هل ينهار شيء؟

### 2) تسجيل الدخول والحساب
1. دخول صحيح، كلمة سر خاطئة، بريد غير موجود، بدون إنترنت — الرسائل عربية ومفهومة.
2. حساب جديد يُجبر على تغيير كلمة السر (must_change_password) ولا يمكن تخطيه بزر الرجوع.
3. **ربط الجهاز:** الدخول بنفس الحساب من جهاز ثانٍ يجب أن يُرفض ("جهاز غير معتمد")، وبعد فك الربط من الأدمن ينجح.
4. تسجيل الخروج ثم الدخول بحساب آخر على نفس الجهاز: لا تبقى أي بيانات من الحساب الأول (الرئيسية، الإشعارات، السلف).
5. انتهاء الجلسة بعد ترك التطبيق ساعات: يعود بسلاسة أو يطلب الدخول بدون انهيار.

### 3) البصمة (أهم جزء — افحصه بعمق)
| الحالة | المتوقع |
|---|---|
| حضور داخل نطاق الفرع | ينجح، يظهر الوقت، الحالة "حاضر" أو "متأخر" حسب الجدول + السماح |
| حضور خارج النطاق | يُرفض برسالة فيها المسافة المتبقية بالمتر |
| على حافة النطاق (±10 م) | سلوك ثابت ومنطقي، بدون تذبذب |
| دقة GPS ضعيفة (داخل مبنى) | رسالة واضحة، لا بصمة خاطئة |
| موقع وهمي (Fake GPS / Developer options) | يُرفض ويُسجَّل كمحاولة تلاعب |
| بصمة حضور مرتين | الثانية تُرفض "سجلت مسبقاً" |
| انصراف بدون حضور | يُرفض برسالة مفهومة |
| انصراف بعد حضور | ينجح، والرئيسية تعرض "اكتمل الدوام" |
| بدون إنترنت | تُحفظ البصمة وتُرسل تلقائياً عند رجوع الإنترنت بوقتها الأصلي |
| بصمة محفوظة أقدم من 48 ساعة | تُرفض برسالة واضحة |
| ساعة الهاتف مقدَّمة يدوياً | لا يُقبل وقت في المستقبل |
| يوم عطلة (مثلاً الجمعة) | السلوك حسب قواعد الشركة وواضح للمستخدم |

تحقق أيضاً: الخريطة تظهر (بدون "API KEY REQUIRED")، دائرة النطاق صحيحة، زر "موقعي" يعمل.

### 4) الإشعارات والتذكيرات
لكل حالة جرّب والتطبيق: **مفتوح / بالخلفية / مغلق تماماً / الهاتف مقفل**:
1. تذكير قبل الدوام بـ 15 دقيقة (بدون بصمة حضور).
2. تذكير بعد بداية الدوام بـ (دقائق التذكير) إذا لم يسجّل حضور.
3. تذكير قبل نهاية الدوام بـ 15 دقيقة (سجّل حضور بدون انصراف).
4. تذكير بعد نهاية الدوام إذا لم يسجّل انصراف.
5. **لا** تذكير لمن عنده إجازة معتمدة اليوم، ولا في يوم عطلة، ولا تكرار لنفس التذكير.
6. إشعار قرار الإجازة/السلفة للموظف، وإشعار "طلب جديد" للأدمن والمدير.
7. التعميم (Announcement): للجميع / لفرع / لموظفين محددين — يصل فقط للمقصودين.
8. الضغط على الإشعار يفتح الشاشة الصحيحة. الصوت والاهتزاز يعملان. العدّاد (Badge) على iPhone يتصفّر.
9. شاشة الإشعارات: مقروء/غير مقروء، التحديث بالسحب، لا يظهر نص تقني أو رموز غريبة.

سجّل وقت وصول كل إشعار (بالثانية) مقارنة بالوقت المتوقع.

### 5) الإجازات
1. إجازة يومية وإجازة بالساعات، مدفوعة وغير مدفوعة، مع مرفق صورة وبدونه.
2. تاريخ نهاية قبل البداية، إجازة في الماضي، تداخل مع إجازة سابقة، تجاوز الرصيد — كلها تُرفض برسائل واضحة.
3. الرصيد يُخصم بعد الموافقة فقط، ويرجع عند الرفض.
4. المدير/الأدمن يوافق ويرفض (مع سبب) — الموظف يستلم إشعاراً ويرى الحالة.

### 6) السلف
**كموظف:**
1. طلب سلفة مع تصوير التعهد (الكاميرا + المعرض) — بدون صورة يُرفض.
2. قسط أكبر من 50% من الراتب يُرفض. طلب ثانٍ مع وجود سلفة جارية يُرفض.
3. جدول الأقساط يظهر مع المسدد/المتبقي بدقة.

**كأدمن (تطبيق + ويب):**
1. اعتماد بتعديل المبلغ/المدة/تاريخ أول قسط — مجموع الأقساط = مبلغ السلفة بالضبط.
2. **سداد مرن** (سلفة 500,000 على 5 أشهر):
   - الشهر 2 دفع 150,000 ← الباقي 100,000 / 100,000 / 50,000
   - الشهر 2 دفع 50,000 ← الباقي 100,000 / 100,000 / 150,000
   - آخر شهر دفع أقل ← يُضاف شهر جديد بالفرق
   - مبلغ أكبر من المتبقي ← يُرفض
   - المعاينة قبل التأكيد = النتيجة بعد الحفظ حرفياً
3. التراجع عن سداد، تأجيل قسط وما بعده شهراً، إعادة الجدولة، طباعة كشف الحساب، حذف سلفة مكتملة.
4. الموظف يستلم إشعاراً بكل دفعة وبكل تعديل.

### 7) الرواتب
1. الأدمن يعتمد كشف راتب: الخصومات (غياب/تأخير) والأقساط المستحقة تُحتسب مرة واحدة فقط.
2. اعتماد نفس الشهر مرتين يُرفض. التراجع عن الكشف يرجع الأقساط غير مدفوعة.
3. الموظف يرى كشفه ويصدّره PDF — الخط العربي سليم والأرقام صحيحة، على Android و iPhone (مشاركة/حفظ).
4. الشهر المؤرشف مقفل للتعديل.

### 8) إدارة الموظفين والفروع (أدمن)
1. إضافة موظف (دور، فرع، راتب، هاتف، مستندات، مولّد كلمة سر) — ثم الدخول به فعلاً.
2. تعديل، تعطيل (لا يستطيع الدخول)، فك ربط الجهاز، رفع/معاينة/حذف مستندات.
3. حذف موظف عليه سلفة غير مسددة يُرفض؛ الحذف الآمن يؤرشف ولا يفقد السجلات المالية.
4. فرع جديد: الموقع من "موقعي الحالي" ومن الضغط على الخريطة، نصف القطر 20–1000 م.
5. جدول الدوام: الأيام، الأوقات، السماح، دقائق التذكير — والتغيير ينعكس على البصمة والتذكيرات.

### 9) التتبع الحي والتقارير
1. التتبع يعمل أثناء الدوام فقط ويتوقف بعد الانصراف. إشعار التتبع ظاهر ونصه صريح.
2. شاشة التتبع للأدمن: مواقع الموظفين تتحدث (كل ~15 ثانية)، الحالات (داخل/خارج/غير متصل) صحيحة.
3. أثر البطارية: شغّل يوم دوام كامل وسجّل نسبة الاستهلاك على كل منصة.
4. تقرير الحضور: الفلاتر، الغياب لا يشمل الموظفين المعطَّلين، فتح موقع البصمة على الخريطة.

### 10) الإعدادات والخصوصية
تغيير الصورة، المستندات، تغيير كلمة السر، رابط سياسة الخصوصية يفتح، رقم النسخة صحيح،
طلب حذف الحساب (يُرفض مع سلفة غير مسددة)، رسالة التحديث الإجباري (OTA) على Android.

### 11) الأمان والصلاحيات (حاول كسرها)
1. الموظف لا يرى بيانات/رواتب/سلف/مواقع غيره بأي طريقة (بما فيها الإشعارات والدليل).
2. المدير لا يستطيع تعديل الموظفين أو الرواتب أو السلف أو ترقية نفسه لأدمن.
3. روابط المستندات والتعهدات لا تُفتح لمن ليس له صلاحية.
4. استعمل Charles/Proxyman إن أمكن: تعديل الطلبات (الإحداثيات، employee_id، الوقت) يجب أن يرفضه السيرفر.

### 12) الواجهة وتجربة الاستخدام
1. **RTL** في كل الشاشات: الأسهم، المحاذاة، الأرقام، التواريخ بالعربي، العملة (د.ع).
2. تكبير الخط من النظام (130% وأكثر): لا نص مقطوع ولا تداخل ولا "overflow" (الشريط الأصفر/الأحمر).
3. شاشات صغيرة (iPhone SE)، كبيرة، تابلت/آيباد، وضع أفقي، الـ Notch والـ Dynamic Island، شريط التنقل بالإيماءات.
4. الوضع الداكن ثابت حتى لو النظام فاتح. التباين مقروء.
5. حالات فارغة، تحميل (Skeleton)، أخطاء مع زر "إعادة المحاولة" — لا شاشة بيضاء ولا رسالة إنجليزية تقنية.
6. زر الرجوع في Android والسحب للرجوع في iPhone لا يضيّعان بيانات نموذج مكتوب بدون تأكيد.

### 13) الاعتمادية
الانتقال بين WiFi والبيانات، وضع الطيران أثناء الإرسال، قتل التطبيق أثناء رفع صورة، إعادة تشغيل الهاتف
(هل الخدمة الخلفية والإشعارات تعود؟)، تغيير المنطقة الزمنية للهاتف، ذاكرة منخفضة.

### فروقات خاصة بكل منصة
- **Android:** قيود البطارية لـ Xiaomi/Samsung/Huawei (Autostart)، إشعار الخدمة الأمامية (Foreground service)،
  Android 14 (نوع خدمة الموقع)، قناة الإشعارات والصوت المخصص، تنصيب APK من مصدر خارجي.
- **iPhone:** صلاحية الموقع "دائماً" والنقطة الزرقاء، Background App Refresh، التركيز (Focus/عدم الإزعاج)،
  إشعارات APNs والتطبيق مغلق، الحفظ والمشاركة لملفات PDF، أول تشغيل بعد التنصيب من TestFlight/Ad Hoc.

---

## صيغة التقرير المطلوبة

### أ) ملخص تنفيذي
عدد الحالات المنفذة، الناجحة، الفاشلة، وأخطر 5 مشاكل بجملة واحدة لكل منها، وحكمك: **جاهز للإطلاق / جاهز بشروط / غير جاهز**.

### ب) جدول الأخطاء (خطأ واحد لكل صف)

| # | الخطورة | المنصة والجهاز | الدور | الشاشة | خطوات إعادة الإنتاج | المتوقع | الفعلي | الدليل | تكرار |
|---|---|---|---|---|---|---|---|---|---|
| BUG-01 | حرج / عالي / متوسط / منخفض | iPhone 12 · iOS 17.5 | موظف | البصمة | 1… 2… 3… | … | … | صورة/فيديو/وقت | دائماً / أحياناً (3 من 5) |

تعريف الخطورة:
- **حرج:** بصمة خاطئة أو ضائعة، خطأ مالي (راتب/سلفة)، تسرب بيانات، انهيار يمنع الاستعمال.
- **عالي:** ميزة أساسية لا تعمل (إشعار لا يصل، طلب لا يُرسل).
- **متوسط:** يعمل بطريقة خاطئة جزئياً أو برسالة مضللة.
- **منخفض:** شكلي (محاذاة، نص، لون).

### ج) نتائج الإشعارات
جدول: نوع الإشعار × حالة التطبيق (مفتوح/خلفية/مغلق/مقفل) × المنصة ← وصل؟ بعد كم ثانية؟

### د) ملاحظات تجربة المستخدم
اقتراحات تحسين غير مصنفة كأخطاء (نصوص محيّرة، خطوات زائدة، أماكن يضيع فيها المستخدم).

### هـ) ما لم يُفحص ولماذا
أي حالة لم تستطع تنفيذها (جهاز غير متوفر، صلاحية ناقصة) — لا تتركها بصمت.

## قواعد مهمة
- كل خطأ يجب أن يكون قابلاً لإعادة الإنتاج من خطواتك فقط. أرفق وقت الحدوث بالدقيقة (لمطابقة سجلات السيرفر).
- افصل الأخطاء: مشكلة واحدة لكل صف، حتى لو كانت في نفس الشاشة.
- لا تحكم "يعمل" على ميزة جربتها على منصة واحدة فقط.
- أي شيء يخص المال (رواتب، أقساط) احسبه يدوياً وقارنه بالرقم الظاهر.
- بعد الانتهاء: احذف كل بيانات `QA-` أو سلّم قائمتها لصاحب المشروع.
